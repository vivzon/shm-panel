# SHM Panel

SHM Panel is a lightweight, secure, and modern web hosting control panel designed for Ubuntu-based VPS and Dedicated servers. It provides a complete stack (Nginx, Multi-PHP, MariaDB, Postfix/Dovecot, FTP, Redis) and features a beautiful WHM (Reseller/Admin) and cPanel (Client) interface.

## 🚀 Features
- **Modern Stack:** Nginx, PHP (8.1, 8.2, 8.3), MariaDB, Redis.
- **Mail Server:** Postfix & Dovecot with Roundcube webmail integration.
- **Database Management:** Automated phpMyAdmin deployment.
- **Security First:** Built-in CSRF protection, secure PDO prepared statements, and SSH key management.
- **Automated SSL:** Let's Encrypt integration for the panel and all subdomains.
- **Performance:** Pre-configured OPCache, Nginx FastCGI caching, and system swap optimization.
- **Beautiful UI:** Built with Vanilla CSS, Lucide Icons, and ApexCharts.

## 📦 System Requirements
- **OS:** Ubuntu 20.04, 22.04 LTS, or 24.04 LTS (Fresh install required).
- **RAM:** Minimum 2GB (4GB+ recommended).
- **Disk:** Minimum 20GB.
- **Network:** A valid domain name pointing to your server's public IP address.
- **User:** Root privileges required.

## 🛠️ Installation Guide

There are three ways to install SHM Panel. The **Unified Installer** is the recommended method for fresh servers.

### Method 1: The Unified Installer (Recommended)
This is a one-click script that prepares your system, clones the latest code, and launches the modular installer.

1. **Run the one-liner on your fresh Ubuntu server:**
   ```bash
   curl -sSL https://raw.githubusercontent.com/vivzon/shm-panel/main/setup.sh | sudo bash
   ```
   *Note: This script handles all dependencies (git, curl, etc.) and clones the project to `/root/shm-panel` automatically.*

### Method 2: The Premium Web Installer
If you have already uploaded the files to your server and have PHP/Nginx running, you can use the beautiful web-based wizard.

1. **Access the installer in your browser:**
   `http://your-server-ip/install.php`
2. **Follow the Multi-Step Wizard** to configure your database and admin account.

### Method 3: The Modular Installer (Manual)
If you prefer to run the steps manually from an existing clone:
1. `chmod +x install.sh installer/*.sh`
2. `./install.sh`

---

## 🔒 Post-Installation Security Steps (CRITICAL)

Once the automated installation is complete, you must secure the panel by running the deployment security script. This script removes leftover installation files and locks down file permissions.

1. **Run the security cleanup script:**
   ```bash
   chmod +x deploy_security.sh
   sudo ./deploy_security.sh
   ```

2. **PHP Configuration (php.ini):**
   To further harden the server, ensure that dangerous PHP functions are disabled at the server level. Run the following command to automatically update all PHP versions:
   ```bash
   sed -i 's/^disable_functions =.*/disable_functions = system,exec,passthru,shell_exec,proc_open,popen/g' /etc/php/*/fpm/php.ini
   systemctl restart php8.1-fpm php8.2-fpm php8.3-fpm 2>/dev/null || true
   ```

---

## 🖥️ Accessing the Panel

After the installation and security lockdown are complete, your services will be automatically secured with Let's Encrypt SSL and available at:

- **Client Panel (cPanel):** `https://client.your-domain.com/`
- **Admin Panel (WHM):** `https://admin.your-domain.com/`
- **Webmail (Roundcube):** `https://webmail.your-domain.com/`
- **Database (phpMyAdmin):** `https://phpmyadmin.your-domain.com/`
- **System Monitor:** `https://monitor.your-domain.com/`

**Default Admin Credentials:**
Check the `/root/shm-credentials.txt` file (if generated by the modular installer) for your auto-generated passwords. If you used the standalone web/vivzon installer, the default username is usually `admin`.

> **Note:** We strongly recommend changing the admin password immediately upon first login.

---

## 🛠️ Recent Patches (v1.1)
- **SSL Automation:** Fixed missing trigger hooks; Certbot now automatically deploys SSL for all panel dashboards upon installation.
- **Application Recovery:** Fixed fatal bugs where Roundcube, phpMyAdmin, and the Monitor dashboard were skipped during installation, resulting in blank pages.
- **Syntax Stability:** Eliminated a fatal Bash syntax anomaly (`unexpected token fi`) in the core `shm-manage` daemon that caused cPanel backend operations to crash.
- **Race Conditions:** Resolved a critical execution sequence error in WHM Account Deletion that historically permanently orphaned Nginx and MariaDB records.
- **Command Bridge Fix:** Resolved a widespread "Unknown command: shm-manage" error caused by the PHP backend double-invoking the `shm-manage` binary when attempting to create Subdomains, FTP accounts, and DNS records.
- **Live Site Override:** Modified the `shm-manage` Nginx VHost engine so the dummy `default.html` ("Coming Soon" page) no longer overrides live custom websites that lack an `index.php` or `index.html` file (e.g. `index.htm`, `app.js`, Node applications).
- **DevOps Nginx Hardening:** Refactored the core Nginx engine in both installers and `/usr/local/bin/shm-manage` to strictly drop default domains (`444`), isolate Host headers, disable server tokens, auto-generate failsafe snippets, and run sequential SSL certification blindly.

> **Note:** We strongly recommend changing the admin password immediately upon first login.

## 🧰 The `shm-manage` CLI Tool

SHM Panel comes with a powerful command-line utility located at `/usr/local/bin/shm-manage` (which must be run as root or via `sudo` by the `www-data` user).

You can use it to manage services directly from the terminal:
```bash
# Add a new SSH key for a user
sudo shm-manage ssh-key add client_username "ssh-rsa AAAA..."

# Clear user logs
sudo shm-manage clear-client-logs client_username

# Check system status
sudo shm-manage status
```

---

## 🛡️ Security Architecture
- Database queries use strict **PDO prepared statements** to prevent SQL injection.
- Passwords are hashed using the modern **BCrypt** algorithm via `password_verify()`.
- Sessions are protected with `Secure` and `HttpOnly` cookie flags and prevent session fixation.
- Shell commands bridging the PHP web interface to the Linux system are strictly sanitized using `escapeshellarg()`.

## 📜 License
This software is provided for private and authorized hosting use.
